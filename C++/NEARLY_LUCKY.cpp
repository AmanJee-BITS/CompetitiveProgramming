#include<iostream>
using namespace std;

int main()
{
    string s;
    cin>>s;
    long long int l= s.length();
    long long int i,c;
    for(i=0;i<l;i++)
    {
      if(s[i]='4' || s[i]='7')
      c++;
    }
    if(c==)
}
